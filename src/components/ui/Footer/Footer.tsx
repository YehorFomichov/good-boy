import React from 'react';
import {FooterContainer} from "./Footer.styles";

const Footer = () => {
    return (
        <FooterContainer>
            
        </FooterContainer>
    );
};

export default Footer;
